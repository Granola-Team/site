steps:
- label: "Prereqs"
  command: |
    echo $HOME
    echo $PATH
    nix-shell ops/shell.nix --run "echo Nix works"
  key: prereqs

- label: "Build"
  command: |
    nix-shell ops/shell.nix --run "netlify build"
  key: build
  depends_on: prereqs

#- label: "Publish"
#  command: |
#    nix-shell ops/shell.nix --run "netlify publish"
#  key: publish
#  depends_on: build
